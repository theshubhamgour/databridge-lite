---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: databridge-namespace
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 32080 # Exposes the frontend on Node IP:32080
